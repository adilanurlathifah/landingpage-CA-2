export default function({ store, redirect }) {
  // const user = store.state.profile.profile;
  // if (user && user?.status?.toLowerCase() === 'inactive') {
  //   console.log(user)
  //   return redirect('/onboarding');
  // }
}